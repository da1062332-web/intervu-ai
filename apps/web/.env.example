# Environment variables for apps/web Next.js app
# Copy this to .env.local in the same directory: cp .env.example .env.local

# URL pointing to the NestJS Backend API
NEXT_PUBLIC_API_URL=http://localhost:3000
