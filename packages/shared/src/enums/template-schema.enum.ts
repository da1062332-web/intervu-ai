export enum VariableType {
  STRING = "STRING",
  NUMBER = "NUMBER",
  BOOLEAN = "BOOLEAN",
  ARRAY = "ARRAY",
  CODE = "CODE",
}

export enum RuleType {
  DIFFICULTY = "DIFFICULTY",
  RANGE = "RANGE",
  LENGTH = "LENGTH",
  REGEX = "REGEX",
}
