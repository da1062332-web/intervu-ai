{
  "id": "tpl_2b8d4e92-d6c1-4f1b-aa31-ea0d1f92ba52",
  "code": "se-lang-js-closures-output",
  "name": "JavaScript Closure Execution Output",
  "templateType": "code-output",
  "topicId": "se-lang-003",
  "conceptId": "Closures & Scope",
  "difficulty": "medium",
  "templateText": "What is the console output of the following JavaScript code execution?\n\n```javascript\nfunction createCounter() {\n  let count = {INITIAL_VALUE};\n  return function() {\n    count += {STEP};\n    return count;\n  };\n}\nconst counter = createCounter();\ncounter();\nconsole.log(counter());\n```",
  "variables": [
    {
      "variableName": "INITIAL_VALUE",
      "variableType": "number",
      "required": true,
      "constraints": {
        "min": 0,
        "max": 10
      }
    },
    {
      "variableName": "STEP",
      "variableType": "number",
      "required": true,
      "constraints": {
        "min": 1,
        "max": 5
      }
    }
  ],
  "active": true,
  "version": 1,
  "outputSchema": {
    "type": "object",
    "required": [
      "questionText",
      "options",
      "correctAnswer",
      "explanation",
      "difficulty",
      "topicId",
      "conceptId",
      "templateId"
    ],
    "properties": {
      "questionText": { "type": "string", "minLength": 15 },
      "options": {
        "type": "array",
        "minItems": 4,
        "maxItems": 4,
        "uniqueItems": true
      },
      "correctAnswer": { "type": "string" },
      "explanation": { "type": "string", "minLength": 20 },
      "difficulty": { "type": "string", "enum": ["easy", "medium", "hard"] },
      "topicId": { "type": "string" },
      "conceptId": { "type": "string" },
      "templateId": { "type": "string" }
    }
  }
}
